import { DateIntervalValidationPipe } from './dateinterval.pipe';

describe('DateIntervalValidationPipe', () => {
  it('should be defined', () => {
    expect(new DateIntervalValidationPipe()).toBeDefined();
  });
});
